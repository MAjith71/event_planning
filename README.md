# README

# Event Planning Platform

## Setup
1. Clone repo
2. cd directory_name
3. Run `bundle install`
4. Run `rails db:migrate`
5. Run `rails server`
6. Open `http://localhost:3000`
7. IF NEEDED install yarn, if already installed then run `yarn add @nathanvda/cocoon`
